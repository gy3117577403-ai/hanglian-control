NODE_ENV=development
PORT=3000
API_PREFIX=api

# 默认继续使用 Mock Repository，不连接真实数据库。
DATA_SOURCE=mock
DEMO_DATA_MODE=empty

# 仅用于后续 Sealos PostgreSQL 测试库接入准备；不要填写生产库。
DB_TARGET=local
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

# 数据库安全闸门：默认全部关闭。
ALLOW_TEST_DB_CONNECT=false
ALLOW_PRISMA_WRITE=false
ALLOW_DESTRUCTIVE_DB_ACTIONS=false

# Seed 预演只生成 JSON，不写入数据库。
SEED_MODE=dry-run
SEED_OPERATOR_ID=system-seed
SEED_OPERATOR_NAME=系统初始化

# V0.8A 只读检查连接参数。
DATABASE_SSL_MODE=require
DATABASE_CONNECT_TIMEOUT_SECONDS=10
